import { ADD_ANIMAL, REMOVE_ANIMAL } from "./actionTypes";

function reducer(state, action) {
    switch (action) {
        case ADD_ANIMAL:

        default:
            return state;
    }
}

export default reducer;